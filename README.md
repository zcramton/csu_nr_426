Prepare Git LFS before accessing this repo to avoid pointer files to the ArcGIS Pro project folder (GlobalMapProject).

For data contact zcramton on GitHub. Lab data is stored in each lesson folder as "Lesson#/L#LabData" and included in .gitignore due to large file sizes.
