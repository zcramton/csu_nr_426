# ============================================================
# requirements.txt
# Beaver Conflict & Restoration Analysis
# Zachary Cramton
# ============================================================

# ============================================================
# CORE DEPENDENCY
# ============================================================
# arcpy ships with ArcGIS Pro and is NOT pip-installable.
# This tool requires ArcGIS Pro 3.x with the Spatial Analyst extension.
#
# To verify your environment from the ArcGIS Pro Python window:
#   import arcpy
#   print(arcpy.GetInstallInfo()["Version"])
#
# Minimum tested: ArcGIS Pro 3.1
# Recommended:    ArcGIS Pro 3.2+
#
# arcpy >= 3.1   (installed with ArcGIS Pro — do not pip install)


# ============================================================
# PYTHON VERSION
# ============================================================
# ArcGIS Pro 3.x ships with Python 3.9 via the arcgispro-py3 conda env.
# This tool requires Python 3.8+ (uses f-strings and type hints).
#
# To check:
#   python --version
#
# python >= 3.9


# ============================================================
# ARCGIS EXTENSIONS REQUIRED
# ============================================================
# Spatial Analyst — required for:
#   arcpy.sa.Reclassify
#   arcpy.sa.ZonalStatisticsAsTable
#   arcpy.sa.Float
#   arcpy.sa.RemapValue
#
# Run validate.py to confirm the extension is available before analysis.


# ============================================================
# STANDARD LIBRARY (no install needed)
# ============================================================
# os
# sys
# datetime


# ============================================================
# INTERNET ACCESS
# ============================================================
# Required only when using REST service URLs for boundary_fc or nlcd_raster.
# Not required if all inputs are local file paths.
#
# Services used:
#   Census TIGER  : tigerweb.geo.census.gov
#   USGS WBD      : hydro.nationalmap.gov
#   MRLC NLCD WCS : dmsdata.cr.usgs.gov


# ============================================================
# OPTIONAL: NON-ESRI PORTING DEPENDENCIES
# ============================================================
# When porting analysis.py to a non-ESRI backend, install these
# in place of arcpy:
#
#   geopandas >= 0.14
#   rasterio >= 1.3
#   rasterstats >= 0.19
#   numpy >= 1.24
#   pandas >= 2.0
#
# Install via ArcGIS Pro conda:
#   conda install -c conda-forge geopandas rasterio rasterstats
#
# Or via pip in a standalone environment:
#   pip install geopandas rasterio rasterstats numpy pandas
